services:
  - type: web
    name: movie-recommender
    env: python
    region: oregon
    plan: free
    branch: main
    startCommand: gunicorn app:app
    envVars:
      - key: TMDB_API_KEY
        scope: run
